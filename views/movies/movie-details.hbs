<h1>Movies</h1>
<a href="/movies/create" class="btn btn-info btn-lg">New Movie</a>


<div class="container">
    <div class="row">
           
            <div class="card mb-3">
                <img src="{{image}}" alt="" class="card-img-top">
                <div class="card-body">
                    <h5 class="card-title">Title: {{title}}</h5>
                    <p class="card-text">Genre: {{genre}}</p>
                    <p class="card-text">
                        <span class="text-muted">Plot: {{plot}}</span>                                
                    </p>
                    <ul>
                        {{#each cast}}
                        <li>
                            {{name}}
                        </li>
                        {{/each}}
                    </ul>
     
                    {{!-- <a class="btn btn-primary" href="/movies/{{_id}}">
                        Details on {{title}}
                    </a>
                    <a class="btn btn-warning" href="/movies/{{_id}}/edit">
                        Edit
                    </a> --}}

                    {{!-- {{>movieCardDetailsButton}} --}}
                    {{>movieCardEditButton}}
                    {{>movieCardDeleteButton}}
                </div>
            </div>   

    </div>
</div>
